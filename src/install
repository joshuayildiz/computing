#!/Users/joshua/code/plan9port/bin/rc

# profile
mkdir -p $home/lib
ln -sf `{pwd}/profile $home/lib/profile

# binaries
chmod +x $home/.bin
go build -o align align.go && mv align $home/.bin
ln -sf `{pwd}/pass $home/.bin/pass
ln -sf `{pwd}/a $home/.bin/a
