#!/Users/joshua/Projects/plan9port/bin/rc

# shell profile
mkdir -p $home/lib
ln -sf `{pwd}/shrc $home/lib/profile

# bin directory
mkdir -p $home/.bin
go build -o align align.go && mv align $home/.bin
ln -sf `{pwd}/pass $home/.bin/pass
